# Description: Platform-independent file locking module
# URL: http://git.openstack.org/cgit/openstack/pylockfile
# Maintainer: Slawomir Gonet, slawek at otwiera dot cz
# Depends on: python python-setuptools

name=python-lockfile
version=0.12.2
release=1
source=(https://pypi.python.org/packages/source/l/lockfile/lockfile-$version.tar.gz)

build() {
	cd lockfile-$version
	python setup.py build
	python setup.py install --prefix=/usr --root $PKG
}
